# Local development (Supabase CLI):
#   supabase start
#   npm run db:migrate
#
# Default local Postgres URL after `supabase start`:
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Dev-only bearer token for POST /api/v1/ingest/transactions (Phase 0.1 spike)
INGEST_DEV_TOKEN=churney_dtk_spike_dev_only
